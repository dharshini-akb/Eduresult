global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'eduresult-backend'
    static_configs:
      - targets: ['host.docker.internal:5000'] # Points to your local backend
